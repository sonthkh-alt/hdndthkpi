# Nhật ký các bước đã làm (cập nhật dần)

> Khi hoàn thành việc lớn, thêm 1 dòng vào đây rồi `git push` để máy khác nắm được.

- **Quản trị điều khiển hiển thị & tên phiên bản (tạm ẩn Cổ điển + Cải tiến)**. Module mới `src/lib/versionCfg.js`: cấu hình `{hidden[], names{}}` mặc định ẩn `classic`/`improved`; lưu TOÀN CỤC ở bảng `app_state` (id=`version_cfg`, upsert, không CAS) → áp dụng cho mọi người truy cập; cache localStorage (`hdndkpi_version_cfg`) đọc nhanh/offline. App: state `versionCfg` + `fetchVersionCfg` khi mở; `vName(id)` (tên tùy chỉnh) dùng ở thanh chọn header, badge "Phiên bản …", tab Hướng dẫn/Quản trị; `shownVersions` — người thường/khách chỉ thấy phiên bản không ẩn, **quản trị (role quantri, gồm bootstrap sonthkh@gmail.com) thấy đủ** (bản ẩn gạch mờ); effect tự chuyển người thường đang đứng ở bản ẩn về bản hiển thị đầu tiên. **Nút bánh răng (chỉ quản trị)** cạnh thanh chọn mở bảng "Quản lý phiên bản": nút Ẩn/Hiện từng bản (không cho ẩn tất cả) + ô đổi tên (lưu khi rời ô, nút ↺ khôi phục tên gốc). `Login.jsx` nhận prop `versionCfg` (fallback `readVersionCfg`): thẻ chọn phiên bản lọc bản ẩn + áp tên tùy chỉnh (màn đăng nhập là trước xác thực nên luôn lọc).

- **Kiểm điểm: dựng lại 2 file Word xuất ra ĐÚNG biểu mẫu docs/DU (Phụ lục 3A & 4)**. Trích cấu trúc thật từ `docs/DU/5.Phlc3A.docx` (python-docx) và `docs/DU/7.Phlc4.doc` (chuyển .doc→.docx qua Word COM rồi đọc). **`exportKiemDiemCaNhan` (Phụ lục 3A)** viết lại theo đúng mẫu: tiêu đề 2 cột (TỈNH ỦY THANH HÓA/ĐẢNG ỦY · ĐẢNG CỘNG SẢN VIỆT NAM/ngày tháng — helper `kdHeaderTwoCol`), khối thông tin (Họ tên/Ngày sinh · Chức vụ Đảng/chính quyền/đoàn thể · Đơn vị), **I. Bảng A Nhóm tiêu chí chung (30đ)** liệt kê ĐẦY ĐỦ từng mục 1.1–3.4 (cột Đảm bảo/Không đảm bảo (x) + Điểm tối đa + Điểm đạt) qua `kdNhomABreakdown`, **Bảng B 6 trục (70đ)** (Nội dung trục+chỉ tiêu · Mục tiêu · Kết quả · KPI% · Điểm tối đa · Điểm đạt) + dòng TỔNG (A)/(B)/(A+B), phần tự kiểm điểm tự luận, **II. Tự đề xuất xếp loại** (4 mức) + ký cá nhân, **III. Nhận xét cấp có thẩm quyền** (Chấm điểm/Đề xuất xếp loại/Mức độ đáp ứng nhiệm vụ then chốt) + ký "XÁC NHẬN CỦA BAN THƯỜNG VỤ ĐẢNG ỦY TRỰC THUỘC TỈNH ỦY…". **`exportKiemDiemTongHop` (Phụ lục 4)** đổi từ bảng điểm cũ sang **7 cột đúng mẫu**: STT · Họ tên · Chức vụ, đơn vị công tác · Cá nhân tự đề xuất mức xếp loại · Cấp có thẩm quyền đề xuất mức xếp loại · Tóm tắt căn cứ (chỉ điền khi HTXS/KHTNV) · Đề xuất nội dung công tác cán bộ; ký "T/M BAN THƯỜNG VỤ ĐẢNG ỦY TRỰC THUỘC TỈNH ỦY HOẶC TẬP THỂ LÃNH ĐẠO". `doKDWord`/`doKDAgg` truyền `nhomA_groups`/`indicators`/`posUnit`/`selfGradeName`/`mgrGradeName`/`reason`.

- **Kiểm điểm: đơn giản hóa cách ghi & đánh giá Nhóm B (6 trục)**. Bỏ danh mục sản phẩm 3B kiểu cũ (10 ô/dòng: cấp trình, sản phẩm, tiến độ, hệ số quy đổi, SL giao/HT, %chất lượng/tiến độ/năng lực LĐ + A/B/C/D). Thay bằng **danh sách nhiệm vụ đơn giản**: mỗi việc chỉ **tên + Mức độ hoàn thành** (dropdown 5 mức `KD_MUC`: Xuất sắc/vượt mức 100% · Hoàn thành tốt 90% · Cơ bản hoàn thành 75% · Chưa hoàn thành 55% · Không hoàn thành 30%) **+ Tầm quan trọng** (`KD_TAM`: Thường ×1 · Quan trọng ×1,5 · Trọng tâm ×2). `trucKPI` = trung bình có trọng số (%) các nhiệm vụ (kẹp ≤100); chưa có việc → mặc định 100. Điều 13: `exceed`=mức Xuất sắc, `fail`=mức Không hoàn thành (<50%), `full`=mọi việc ≥ Hoàn thành tốt → suy `exceedRate`/`failRate`/`allFull`. `trucTasks(d)` **tương thích ngược**: dữ liệu cũ có `products` tự quy đổi sang nhiệm vụ đơn giản (theo tỷ lệ HT/SL & hệ số). Gộp 2 ô Mục tiêu/Kết quả → 1 ô `note` (không bắt buộc). Component `ProductList`→`TaskList` (name + 2 dropdown). `defaultKD` seed theo hồ sơ A/B/C/D bằng chuỗi mức. Word `doKDWord` liệt kê "tên — mức" mỗi trục. Cập nhật Hướng dẫn mục 3.

- **Kiểm điểm: đối chiếu QĐ 73-QĐ/TU + bổ sung Bản kiểm điểm tự luận & siết điều kiện xếp loại (Điều 13)**. Nghiên cứu 2 file mới trong `docs/DU/` (QĐ 73-QĐ/TU 14/11/2025 scan + bộ 70 trang biểu mẫu) bằng cách render PNG (pymupdf) do PDF scan không trích text. Kết luận: bản Kiểm điểm (theo HD 03 hằng quý) ĐÚNG mạch QĐ 73 (30/70, 4 mức, Mẫu 07/08). Người dùng chọn GIỮ khung 6 trục (HD 03) + thêm 2 việc: (1) **Bản tự kiểm điểm (tự luận, Mẫu 2A/07)**: thêm `kd.uudiem`/`hanche`/`phuonghuong` (ưu điểm/hạn chế & nguyên nhân/phương hướng khắc phục) — section riêng trên phiếu + đưa vào Word (`exportKiemDiemCaNhan` mục II. TỰ KIỂM ĐIỂM). (2) **Điều kiện xếp loại Điều 13**: `evalKDGrade` thay `gradeFromTotal` — HTXS cần ≥90 + hoàn thành 100% (full) + **≥30% nhiệm vụ vượt mức** (`exceedRate`); **>50% nhiệm vụ không HT** (`failRate`, HT<50% số lượng) → KHT; ô **"bị kỷ luật/suy thoái"** (`kd.disciplined`) ép KHT; hiển thị `gradeReasons`. Dashboard thêm **cảnh báo trần HTXS ≤ 20% số HTT**. exceedRate/failRate suy từ danh mục sản phẩm. KHÔNG đổi sang khung "Cơ quan dân cử" (Phụ lục 03/04) vì đó là khung hằng năm.

- **Kiểm điểm: dựng Nhóm B đúng phương pháp Phụ lục 3B + chỉ tiêu Phụ lục 1A + danh mục đặc thù cơ quan**. (1) `KD_TRUC` thêm `indicators[]` = chỉ tiêu/nội dung chính thức từng trục (Phụ lục 1A) — hiển thị mục "Chỉ tiêu, nội dung của trục" (mở/gập). (2) **`trucKPI(d)`**: KPI mỗi trục **tự tính từ danh mục sản phẩm** — planQ=Σ(SL×hệ số); A=Σ(HT×hệ số)/planQ; B/C/D=Σ(HT×hệ số×%chất lượng/tiến độ/lãnh đạo)/planQ; KPI=(A+B+C+D)/4 (kẹp ≤100). Nếu chưa lập danh mục → dùng A/B/C/D nhập tay (mặc định 100). `computeKD` gọi `trucKPI`. (3) **`KD_TRUC_SAMPLE`** dựng theo cấu trúc Phụ lục 3B (Nhiệm vụ · Cấp trình · Sản phẩm · Số lượng · Tiến độ · Hệ số quy đổi) với nhiệm vụ/sản phẩm **đặc thù Văn phòng Đoàn ĐBQH & HĐND tỉnh** (thẩm tra, giám sát chuyên đề, tiếp công dân/xử lý đơn thư, thẩm tra nghị quyết, CĐS họp không giấy, sinh hoạt cấp ủy, giám sát an sinh/QP-AN, đối ngoại…). (4) `ProductList` đầy đủ: kế hoạch (SL giao × hệ số) + thực tế (SL hoàn thành, %chất lượng, %tiến độ, %lãnh đạo); cảnh báo khi HT<định mức; trục hiển thị A/B/C/D read-only tự tính. `defaultKD` sinh thực tế theo hồ sơ A/B/C/D.

- **Thêm phiên bản thứ NĂM "Kiểm điểm" (đánh giá hằng QUÝ theo HD 03-HD/TU 02/7/2026)** — tách biệt như bản Singapore, module riêng `src/KiemDiemAppraisal.jsx`. Nghiên cứu tài liệu người dùng cung cấp trong `docs/DU/` (HD 03-HD/TU + Phụ lục 1A/1B/2/3A/3B/4). **Đối tượng**: cán bộ diện BTV Tỉnh ủy quản lý (2 PCT HĐND, 4 Trưởng Ban + 4 Phó Ban KTNS/VHXH/Pháp chế/Dân tộc, Chánh VP + 2 Phó Chánh VP). **Mô hình**: thang 100 = **Nhóm A (30đ)** tiêu chí chung 3 nhóm (10+10+10) chấm NHỊ PHÂN (Đảm bảo=đủ điểm/Không=0) + **Nhóm B (70đ)** theo **6 TRỤC kết quả trọng tâm** (15/10/10/15/10/10), mỗi trục **Điểm = KPI% × điểm tối đa**, **KPI = (A+B+C+D)/4** (số lượng/chất lượng/tiến độ/năng lực lãnh đạo); tùy chọn lập danh mục sản phẩm/công việc theo trục (hệ số quy đổi). Xếp loại 4 mức HTXS/HTT/HT/KHT (HTXS cần ≥90 & không thiếu nhiệm vụ; cảnh báo Điều 6.2 <100% → KHT trừ lý do khách quan). `computeKD`/`defaultKD`/`kdGradeInfo`/`KiemDiemAppraisal`(phiếu cá nhân)/`KiemDiemDashboard`(phân bố + Bảng tổng hợp Phụ lục 4). **Chu kỳ theo QUÝ** (I–IV): header đổi ô tháng → dropdown Quý (map month 3/6/9/12), `quarterLabel`. **Tab gọn 3 module** (Tổng quan/Đánh giá/Hướng dẫn) như SonHa. **Xuất Word**: `exportKiemDiemCaNhan` (Bản tự đánh giá cá nhân — Phụ lục 3A) + `exportKiemDiemTongHop` (Bảng tổng hợp tập thể — Phụ lục 4, khổ ngang) trong `exporters.js`. Đăng ký `'kiemdiem'` (VERSIONS/VERSION_THEME đỏ đô, Root VALID, Login card, `person.kd`, seed 13 đồng chí). App: `isKD`, `scoreOf=isKD?computeKD:...`, `result=kdGradeInfo`, render eval/dashboard riêng, ẩn Nhóm đối tượng/aside cổ điển; tab Hướng dẫn có mục KD riêng.

- **Ẩn tab "Quản trị" ở mọi phiên bản**: bỏ tab Quản trị khỏi thanh tab (trước chỉ SonHa ẩn; nay cả Cổ điển/Cải tiến/Singapore đều ẩn) + redirect về Tổng quan nếu đang ở tab 'admin'. Tab **Danh mục** vẫn giữ cho Cổ điển/Cải tiến (quản trị). Lưu ngay/Thêm cán bộ vẫn dùng bình thường qua header và panel Đánh giá.

- **SonHa: Nhóm đối tượng (Mẫu) TỰ SUY theo chức vụ + seed 20 cán bộ + thưởng vượt định mức**. (1) **Bỏ Mẫu 01/02 kiểu đại biểu** trong bản SonHa; đánh số lại 4 Mẫu theo chức vụ (`SONHA_MAU`/`sonhaMauKey`/`sonhaTypeOf`): **Mẫu 01 — Lãnh đạo, quản lý 01** (Chánh/Phó Chánh VP → Nhóm II chỉ `SH.LD` 01–08) · **Mẫu 02 — Lãnh đạo, quản lý 02** (Trưởng/Phó phòng → như "lãnh đạo, quản lý" hiện tại = `SH.CM`+`SH.LD`) · **Mẫu 03 — Công chức** (chuyên viên → `SH.CM`) · **Mẫu 04 — Lao động hợp đồng** (`SH.HT`). Khối "Nhóm đối tượng" ở tab Đánh giá (bản SonHa) chuyển thành **thẻ hiển thị Mẫu tự động** (không chọn tay); đổi **Chức vụ** tự đổi `type` + Mẫu; dropdown Nhóm II lọc theo `sonhaGroupsOf(person)`. Thêm chức danh hợp đồng (Lái xe/Bảo vệ/Nhân viên phục vụ) vào `ORG_UNITS` (Phòng HC-TC-QT). (2) **Seed 20 cán bộ SonHa** (`seedSonHaPeople`, `seedDemoPeople(version)`): 1 Chánh + 1 Phó Chánh + 4 Trưởng phòng + 4 Phó phòng + 8 chuyên viên (mỗi phòng 2) + 2 lao động hỗ trợ, mỗi người ~10 nhiệm vụ tiêu biểu theo danh mục Mẫu (`genTasksFromCat`). (3) **Xóa nút "Thu thập nhiệm vụ từ Bảng theo dõi CV"** trong bản SonHa. (4) **Vượt định mức + thưởng điểm**: `agg335` tính `exceedPct` (phần hoàn thành > định mức, trọng số theo hệ số); mỗi nhiệm vụ vượt có **badge "▲ vượt +N"**; `computePerson` cộng **điểm thưởng SonHa** = +0,1đ/1% vượt bình quân, **trần +5đ** (tổng vẫn ≤100), CHỈ áp bản SonHa (`ACTIVE_VERSION`); hiển thị dòng "▲ Điểm thưởng vượt định mức (+x%) + y" ở khối tổng hợp + ghi chú giải thích ở Nhóm II. Nghiên cứu: NĐ 335 kẹp a,b,c ≤100 và coi ≥30% vượt mức là điều kiện loại A, cho phép "điểm cộng theo quy chế cơ quan" (Chương VI.2 đặc tả); thưởng nhỏ có trần để tránh Goodhart (khai khống).

- **Thêm phiên bản thứ tư "SonHa" (gọn 3 module) + danh mục VP theo NĐ 335/2025**: nạp tài liệu người dùng cung cấp trong `docs/` (Đặc tả yêu cầu phần mềm, Engine tính điểm mô phỏng, 3 QĐ Danh mục sản phẩm/công việc). Dựng **`SONHA_CATALOG`** trong `App.jsx` từ QĐ Danh mục VP Đoàn ĐBQH & HĐND tỉnh Thanh Hóa — 48 mục: 34 CMNV (Phần I, nhóm A→G) + 8 LĐQL (Phần II) + 6 HTPV (Phần III), mỗi mục có `types[]` (CMNV→staff/leader/hdnd/dbqh, LĐQL→leader/hdnd/dbqh, HTPV→contract), `level` N1–N5 theo nhóm độ phức tạp, `maxScore` = "Điểm" quy đổi (đơn vị chuẩn 100 = hệ số 1,0 → dùng làm trọng số trong `agg335`). Thêm `ACTIVE_BASE`/`setBaseCatalog(v)` để `findCatalogItem`/`getND335Groups`/`catalogForGuide` đổi danh mục nền theo phiên bản (SonHa dùng `SONHA_CATALOG`, các bản khác dùng `CATALOG`). Đăng ký phiên bản `'sonha'` (VERSIONS/VERSION_THEME xanh lá, `setCriteriaVersion`→Nhóm I dùng câu chữ pháp lý CRITERIA_CLASSIC, `Root.jsx` VALID, `Login.jsx` thẻ chọn). **SonHa chỉ gồm 3 module**: Tổng quan · Đánh giá · Liên hệ & hướng dẫn (ẩn Năng lực số/Theo dõi CV/Danh mục/Quản trị + redirect tab). Tab Đánh giá thêm component `SonHaConnectors` — 2 mục **để chờ cấu hình**: (1) **Liên kết hệ thống "Quản lý văn bản"** (lấy nhiệm vụ giao/văn bản phát hành làm minh chứng — nút Kết nối disabled, cấu hình endpoint sau) và (2) **Import file đánh giá** (chọn file .xlsx/.csv/.json khi không muốn chấm trực tiếp trên web — ánh xạ vào phiếu khi ban hành mẫu). Cách tính điểm giữ nguyên khung 30/70 + Điều 8 (khớp Chương VI đặc tả: (a+b+c)/3, lãnh đạo (a+b+c+d+đ+e)/6). Đổi phiên bản không mất điểm Nhóm I (cùng id/max).

- **Vào thẳng bằng tài khoản khách (bỏ cổng đăng nhập)**: mặc định `session='guest'` khi chưa đăng nhập thật (`getSession() || 'guest'`, `onAuthChange(ns||'guest')`) → không hiện màn Login khi truy cập. Khách XEM/DEMO mọi nội dung, thử chấm điểm Nhóm I/II (in-memory) nhưng **không ghi DB** (autosave/Lưu ngay đã chặn cho guest). Thêm state `wantLogin` + nút "Đăng nhập" (góc phải) để quản trị chủ động đăng nhập; Login nhận `onClose` ("← Quay lại xem demo"). Đăng xuất quay về khách.

- **Tổng quan thêm đồ thị**: `DashboardCharts.jsx` bổ sung **đồng hồ (radial gauge) Điểm TB cơ quan** + xếp loại, và **radar Khung năng lực số** (TB cơ quan, 8 trục N1–N8, thang Mức 0–4). App.jsx truyền `digital={DIGITAL}`. Charts dùng gradient + theme (hiện chỉ 1 bản Cổ điển).

- Hợp nhất đánh giá NĐ335 vào tab Đánh giá (Nhóm II theo danh mục + hệ số).
- Đăng nhập **email + mật khẩu**: lần đầu nhận liên kết → nhập Họ tên/Chức vụ + tạo mật khẩu; có đổi mật khẩu.
- **Tài khoản khách** `user@thanhhoa.gov.vn` / `password`: hiện sẵn ở trang login; được thử chấm điểm Nhóm I/II nhưng **không lưu** (mất khi tải lại).
- Cán bộ tự chọn **Phòng/Bộ phận** và **Nhóm đối tượng** của chính mình.
- **Theo dõi CV**: nút **Thu thập vào đánh giá KPI** (tạo nhiệm vụ Nhóm II từ dòng theo dõi, idempotent qua `srcTrkId`); **Xuất bảng (PDF)** qua cửa sổ in; **Đồng bộ từ Google Sheet** (`/api/kiemdem`) nạp thành dòng theo dõi sửa được (idempotent qua `fromSheet`).
- Module **Liên hệ & hướng dẫn**: thông tin liên hệ (đ/c Hà Ngọc Sơn, 0904818886) + ô gửi ý kiến (mailto `sonthkh@gmail.com`); trang hướng dẫn cập nhật theo app.
- **Thiết kế lại quốc huy + 3 phiên bản giao diện** chọn ở Login (mặc định Cổ điển): Cổ điển (đỏ), Mới (`AppModern.jsx`, tối nebula), PRO (`AppPro.jsx`+`lib/pro.js`, trắng-đen). `Root.jsx` điều phối, `main.jsx` render Root.
- **Danh mục Phòng/Chức vụ (`ORG_UNITS`)**: ô Phòng + Chức vụ ở màn Đánh giá chuyển thành **dropdown** (cả 3 bản). **Cán bộ mới mặc định 100/100** (đánh giá trừ dần).
- **Ẩn hệ số/trọng số khi chấm** (cả 3 bản) — chỉ giải thích ở tab Hướng dẫn.
- **Bản PRO tính ĐÚNG NĐ335 + Sổ tay**: đếm khách quan, hệ số quy đổi N1–N5, danh mục theo Phòng/Vị trí (`WORK_CATALOG_PRO`); lãnh đạo (a+b+c+d+đ+e)/6 theo chức vụ; hợp đồng theo Sổ tay Chương III (3 tiêu chí).
- **5 nhóm đối tượng, đánh số Mẫu 01–05** (`CRITERIA_ORDER`): 01 ĐB HĐND chuyên trách · 02 ĐB Quốc hội chuyên trách · 03 Lãnh đạo, quản lý · 04 Công chức · 05 Lao động hợp đồng.
- **Bản demo nội bộ**: nhãn "Bản demo thử nghiệm" + dòng miễn trừ trách nhiệm ở header/footer/login.
- **Tổng quan trực quan (Recharts)**: thêm `src/lib/DashboardCharts.jsx` (lazy-load) — donut cơ cấu xếp loại, bar xếp hạng điểm cán bộ, biểu đồ xu hướng theo kỳ (cột chồng A/B/C/D + đường điểm TB); thêm bảng **Tổng hợp theo Phòng/Bộ phận** (`src/lib/dash.js` `deptSummary`). Gắn cho CẢ 3 phiên bản (App/Modern/Pro), theme sáng/tối qua prop `dark`.
- **Biểu đồ theo tông từng phiên bản**: `DashboardCharts` đổi prop `dark`→`theme` (classic đỏ-vàng / modern tím-xanh / pro xanh Matrix nền tối); thêm gradient (donut bo góc, bar, cột so sánh), Xu hướng dùng Area gradient. Bản PRO đảo sang nền đen chữ trắng phong cách Matrix (AppPro + Login theme pro).
- **So sánh Chất lượng & KPI theo Phòng/Ban**: `deptSummary` bổ sung `quality`(Nhóm I /30), `kpi`(Nhóm II /70), `qualityPct/kpiPct`; thêm biểu đồ cột nhóm so sánh % đạt giữa các đơn vị + 2 cột Chất lượng/KPI trong bảng theo Phòng (cả 3 bản).
- **Tổ chức bộ nhớ dự án**: tách `CLAUDE.md` thành các file chủ đề trong `.claude/rules/` (theo Claude Code rule spec); mở ngoại lệ `.gitignore` để commit thư mục này.
- **Cập nhật toàn diện theo QĐ 1053-QĐ/TU (05/6/2026) tỉnh Thanh Hóa** (`11.pdf`): dựng lại bộ tiêu chí Nhóm I (30đ) đúng văn bản — `staff` chuyển từ 15/10/5 sang **5+5+16+4** (Phụ lục 01); `leader` mục 3 tách thành **6 tiểu mục** (NL lãnh đạo 3 · chuyên môn 3 · thực thi 2 · tác phong 2 · đổi mới 3 · CCHC/CĐS 3) (Phụ lục 03); `contract` giữ 15/10/5 (Phụ lục 02). Vì `CRITERIA` dùng chung nên cả 3 bản đều được chuẩn hóa. Thêm `isLeaderPerson` vào `App.jsx` (pro.js import lại). **Bản Cổ điển + Mới: lãnh đạo tính Nhóm II theo (a+b+c+d+đ+e)/6** (Điều 7) — thêm ô nhập d/đ/e (100%/50%). **Tự động áp điều kiện xếp loại Điều 8**: `classify`→`GRADES`+`gradeClass`, thêm `taskStats`+`evalGradeCode` (HTXS cần 100% HT & ≥30% vượt mức; ô "bị kỷ luật"→KHTNV; >50% nhiệm vụ không HT / lãnh đạo <70%→KHTNV) + hiển thị lý do hạ mức; `dist`/bảng/dept dùng `c.grade`. Cập nhật tab Hướng dẫn (Mẫu 01–05, công thức lãnh đạo, điều kiện Điều 8, mốc tháng 12 trước 15/12).
- **Quản trị quản lý Danh mục công việc (Nhóm II)**: thêm registry dùng chung trong `App.jsx` (`CUSTOM_CATALOG`/`HIDDEN_CATALOG`, `setCatalogRegistry`, `findCatalogItem`, `LEVEL_SCORE`); `getND335Groups` bỏ mục ẩn + thêm mục tùy chỉnh theo `types[]`; `agg335` tra hệ số qua `findCatalogItem`. Bản Cổ điển có **tab "Danh mục"** (chỉ Quản trị) — component `CatalogManager`: thêm/sửa/xóa công việc tùy chỉnh + **gán Nhóm đối tượng (Mẫu 01–05)** + chọn cấp độ N1–N5 (hệ số 100–500), và **ẩn/hiện** công việc mặc định ("bớt"). Danh mục lưu theo kỳ trong `state.catalog = {custom[],hidden[]}`. **Cả 3 bản round-trip `catalog`** khi lưu (tránh ghi đè mất khi đổi phiên bản): Cổ điển + Mới đồng bộ registry & hiển thị mục tùy chỉnh trong dropdown; PRO giữ qua `catalogRef` (PRO dùng danh mục theo Phòng riêng).
- **Sửa đầy đủ thông số danh mục (kể cả mục mặc định)**: thêm lớp `overrides` (`state.catalog.overrides[id]`) — `setCatalogRegistry`/`findCatalogItem`/`getND335Groups` áp ghi đè + `effectiveTypes`/`applyOverride`; `getND335Groups` gộp mặc định + tùy chỉnh, lọc theo nhóm đối tượng HIỆU LỰC. `CatalogManager` dùng component `CatalogRow` dùng chung: sửa được **tên, nhóm/phân loại, sản phẩm đầu ra, cấp độ/hệ số, Nhóm đối tượng** cho cả mục tùy chỉnh lẫn mục mặc định; mục mặc định đã sửa có viền vàng + nút "↺ Khôi phục mặc định"; vẫn giữ ẩn/hiện. `defaultTypesOfId` chuyển lên module-scope.
- **Giải thích rõ điều kiện xếp loại trong tab Đánh giá + làm rõ "bị kỷ luật"**: thêm component `GradeExplain` (bản Cổ điển) hiển thị ngay khi chấm — mức hiện tại, các chỉ số sống (% hoàn thành/vượt mức/chậm tiến độ vs ngưỡng), 4 mức A/B/C/D kèm điều kiện, lý do hạ mức hoặc xác nhận đạt đủ. Bản Mới thêm bảng chỉ số + lý do tương tự. **Chốt hành vi "bị kỷ luật" (Điều 8.4): chỉ ép xếp loại KHTNV, KHÔNG trừ vào tổng điểm** (độc lập thang điểm) — ghi rõ ở ô tick, ở `GradeExplain` và tab Hướng dẫn; ô "Điểm trừ" là cơ chế trừ điểm riêng (trừ thẳng vào tổng).
- **Sửa logic "không hoàn thành nhiệm vụ" — xét theo TỪNG nhiệm vụ**: trước đây HT≠Giao (vd 9/10) bị coi là cả nhiệm vụ không hoàn thành → ép loại D sai dù điểm cao. Nay `taskStats` đổi sang tỷ lệ r=HT/Giao mỗi nhiệm vụ: `doneRate` (r≥1), `exceedRate` (r>1), `failRate` (r<0.5 = KHÔNG hoàn thành), `delayRate`. `evalGradeCode`: D khi `failRate`>50% (lãnh đạo >30%); A cần `doneRate`=100% & `exceedRate`≥30%; B→C khi có nhiệm vụ <50%. `GradeExplain`/bản Mới: tách danh sách "Không hoàn thành (<50%)" vs "Chưa đạt đủ số lượng (50–99%)" vs "Chậm tiến độ"; đổi chỉ số hiển thị (Đạt đủ SL/Vượt mức/Không HT). Cập nhật Hướng dẫn mục 7 nêu rõ ngưỡng 50%/nhiệm vụ.
- **Tối ưu hệ thống — Phase A (hiệu năng & dung lượng tải)**: `Root.jsx` lazy-load bản Mới/PRO qua `React.lazy`+`Suspense` (mặc định cổ điển không còn tải mã 2 bản kia); `vite.config.js` thêm `manualChunks` tách `react-vendor` + `supabase` để cache tốt + nâng `chunkSizeWarningLimit`. Bỏ `refreshTrends()` (gọi `loadAllPeriods` rất nặng) sau mỗi lần "Lưu ngay" ở cả 3 bản — chỉ giữ ở lần mở app + nút "Làm mới". Kết quả build: chunk `index` 639 kB → ~181 kB; AppModern ~58 kB, AppPro ~61 kB (chunk lazy).
- **Tối ưu hệ thống — Phase B (độ bền vững)**: `computePerson`/`computePro` phòng vệ khi `!p || !CRITERIA[p.type]` (trả object an toàn, không crash cả app); cũng phòng vệ `selfScores`/`mgrScores` thiếu. Thêm `clampPeriod` (tháng 1–12, năm 2020–2100) dùng trong `loadPeriod` cả 3 bản — gõ nhầm tháng/năm tự sửa lại, không nạp kỳ rác. Nhiệm vụ Nhóm II **chưa chọn danh mục** giờ có badge "chưa tính điểm" (viền vàng ô chọn) + nhắc số lượng trong `GradeExplain` — làm lộ rõ việc bị bỏ khỏi tính điểm (cả Cổ điển & Mới).
- **Tối ưu hệ thống — Phase C bước 1 (dọn mã)**: xóa component chết `PersonChips` (App.jsx, không nơi nào render). Thêm helper dùng chung `taskBreakdown(tasks)` → `{failed(<50%), partial(50–99%), delayed, uncounted}`; `GradeExplain` (Cổ điển) và hộp điều kiện (bản Mới) dùng chung thay vì mỗi nơi tự lọc — bớt trùng lặp logic phân loại nhiệm vụ.
- **Gỡ bỏ bản Mới & bản PRO — chỉ còn 1 giao diện (Cổ điển)**: xóa `src/AppModern.jsx`, `src/AppPro.jsx`, `src/lib/pro.js`, `src/Root.jsx`. `main.jsx` render thẳng `<ErrorBoundary><App/></ErrorBoundary>`. `Login.jsx` bỏ bộ chọn 3 phiên bản + theme modern/pro (chỉ còn tông cổ điển), bỏ props `version`/`onPickVersion`. `App.jsx` bỏ props `version`/`onPickVersion` và khối `export {...}` model dùng chung (không còn ai import). Mã 2 bản đã gỡ vẫn còn trong lịch sử git nếu cần khôi phục. Build: chunk `index` ~178 kB, không còn chunk AppModern/AppPro.
- **Phê duyệt & xuất phiếu Word đầy đủ**: thêm cơ chế phê duyệt của cấp có thẩm quyền (`person.approved/approvedBy/approvedRole/approvedAt`, lưu theo kỳ, reset khi sang kỳ mới/đặt lại; `upCur` tự GỠ phê duyệt khi sửa trường chấm điểm). Aside tab Đánh giá thêm bảng trạng thái "Đã/Chưa phê duyệt" + nút **"Phê duyệt & xuất phiếu"** (cấp duyệt) / **"Bỏ phê duyệt"**. Viết lại `exportWordPhieu` (exporters.js) thành phiếu Word **chi tiết, có bảng**: thông tin cán bộ · bảng Nhóm I từng tiêu chí (Tự ĐG/Cấp duyệt + cộng) · bảng Nhóm II từng nhiệm vụ (danh mục, SL giao/HT, lỗi CL, trễ, tỷ lệ %, điểm %; nhiệm vụ chưa chọn danh mục được tô đỏ "không tính điểm") · thành phần lãnh đạo d/đ/e · bảng tổng hợp điểm & xếp loại · điều kiện Điều 8 (gradeReasons) · ghi chú kỷ luật · nhận xét cá nhân/cấp duyệt · trạng thái phê duyệt + 2 khối chữ ký. `doWord` dựng dữ liệu chi tiết từ `CRITERIA`/`findCatalogItem`/`task335Score`.
- **Sửa hiển thị/tính điểm Nhóm I vượt trần**: kẹp điểm theo `it.max` khi ĐỌC (computePerson + tab Đánh giá + xuất Word) để dữ liệu cũ lưu trước khi đổi bộ tiêu chí (vd mục 3.5 từng có max cao hơn) không còn hiện/tính vượt điểm tối đa mới.
- **Sổ tay hướng dẫn (PDF) tải xuống**: thêm `exportGuidePDF(unit)` (exporters.js) — tài liệu A4 dọc đầy đủ (trang bìa + mục lục + 14 mục: giới thiệu/QĐ 1053, 5 tab, thang điểm, Nhóm I theo Mẫu 01–05, Nhóm II + công thức + ví dụ + lãnh đạo d/đ/e, hệ số N1–N5, 4 mức xếp loại & Điều 8 theo từng nhiệm vụ, kỷ luật vs điểm trừ, quy trình & mốc thời gian, phê duyệt & xuất Word, Theo dõi CV, phân quyền, lưu theo kỳ, FAQ) qua cửa sổ in (tiếng Việt chuẩn). Tab Hướng dẫn thêm nút "Tải sổ tay hướng dẫn (PDF)".
- **Thêm phiên bản "Singapore" (chọn ở Đăng nhập) — lai ghép câu hỏi dễ hiểu + mô hình công chức Singapore**: khôi phục cơ chế chọn phiên bản qua `src/Root.jsx` (giữ lựa chọn trong `localStorage` khóa `hdndkpi_version`); `main.jsx` render `<Root/>` thay vì `<App/>`. `App.jsx` nhận props `version`/`onPickVersion`; tách `CRITERIA` cũ thành **`CRITERIA_CLASSIC`** (QĐ 1053, câu chữ pháp lý) + thêm **`CRITERIA_SG`** (CÙNG id/`max`/thang 30đ/công thức → mọi tính toán, clamp, Điều 8, xuất phiếu dùng chung, đổi phiên bản KHÔNG mất điểm) nhưng VIẾT LẠI câu hỏi theo phong cách khu vực công Singapore (mỗi câu "Anh/chị có…?" + ví dụ), ánh xạ **AIM** (Analytical/Influence/Motivation) · **ISE** (Integrity/Service/Excellence) · **WoG** (Whole-of-Government, lấy người dân/đại biểu làm trung tâm). Biến `CRITERIA` "active" + `setCriteriaVersion(v)` (mẫu giống `setCatalogRegistry`); App gọi `setCriteriaVersion(version)` đầu render + thêm `version` vào deps `computed`. UI: header đổi tông xanh teal + nhãn "Phiên bản Singapore" + nút gạt Cổ điển/Singapore; `Login.jsx` thêm 2 thẻ chọn phiên bản; tab Đánh giá hiện panel giới thiệu AIM/ISE/WoG khi ở bản SG. Nghiên cứu nguồn: PSD Singapore (CEP, AIM — không phải HAIR/Helicopter cũ), CSC (giá trị ISE 2003), MOF SPOR 2024 (KPI hướng kết quả), GovTech (OKR). Bản Cổ điển giữ nguyên để dùng pháp lý.
- **3 phiên bản tiêu chí (Cổ điển · Cải tiến · Singapore dân cử) + Nhóm II gom theo Mục tiêu (OKR)**: đổi tên bản "Singapore" cũ → **`CRITERIA_IMPROVED`** (Cải tiến, giữ Nhóm I kiểu AIM/ISE). Thêm **`CRITERIA_SG`** MỚI (Singapore *cơ quan dân cử*) cho cả 5 nhóm, cùng id/`max`/thang điểm/công thức (đổi phiên bản không mất điểm) nhưng câu hỏi nhấn chức năng **đại diện · giám sát · lập pháp/nghị quyết · tiếp xúc cử tri**; đại biểu (Mẫu 01/02) dùng `dancuGroups` riêng. `setCriteriaVersion(v)` 3 nhánh (classic/improved/sg); `Root.jsx` + `localStorage` nhận 3 giá trị; `Login.jsx` + header chọn 3 phiên bản; theme màu theo phiên bản (`VERSION_THEME`/`VERSIONS`): đỏ/teal/tím. **Nhóm II (bản Cải tiến & Singapore)**: thêm trường `kr` ("Kết quả cần đạt") cho mỗi nhiệm vụ; **gom nhiệm vụ theo Mục tiêu (OKR)** qua `renderGroupedTasks`/`renderTask335Row`, có nhóm "việc thường xuyên / chưa gắn mục tiêu" (gắn mục tiêu là **khuyến khích, không bắt buộc**); `newTask335(objId)` nhận mục tiêu mặc định. **Nghiên cứu OKR (Google/Doerr, GovTech SG, IPU, quy luật Goodhart)** → kết luận áp dụng: **OKR chỉ để ĐỊNH HƯỚNG/alignment, KHÔNG dùng % đạt OKR để tính điểm/xếp loại** (tránh đặt mục tiêu thấp); KR nên ở cấp Mục tiêu cơ quan, ô KR ở nhiệm vụ chỉ mô tả sản phẩm/chỉ tiêu. Ghi rõ điều này ở panel tab Đánh giá + tab Hướng dẫn mục 11. Phiếu Word: ô nhiệm vụ Nhóm II thêm "KR: …" và "Mục tiêu: …".
- **OKR đầy đủ: Key Results cấp Mục tiêu (tab Tổng quan)**: `objectives[]` thêm `krs: [{id,text,target,current,unit}]`. Helper `krPct`/`objKrGrade`/`krTone` + `KR_TONE` (màu literal tránh purge) + `upObjective`/`addKr`/`upKr`/`delKr` (counter `krSeq`). Mỗi mục tiêu hiển thị danh sách Key Result (sửa hiện tại/chỉ tiêu/đơn vị, thanh tiến độ %, vùng tốt 60–70% xanh) + badge **OKR %** (trung bình KR) tách bạch với thanh **Thực thi nhiệm vụ** (objProgress cũ). Quản trị thêm/xóa KR & mục tiêu. **KR/OKR CHỈ theo dõi tiến độ cơ quan — KHÔNG cộng vào điểm cá nhân** (không đụng computePerson; đúng kết luận nghiên cứu Google/Doerr/Goodhart). 3 mục tiêu mẫu có sẵn KR. Hướng dẫn mục 5 viết lại (Objective + Key Results, 60–70%, không dùng chấm điểm, gắn mục tiêu không bắt buộc).
- **Bản Singapore = mô hình đánh giá Singapore THẬT (thay toàn diện) + xuất Word riêng**: tạo `src/SingaporeAppraisal.jsx` — bản 'sg' KHÔNG dùng khung 30/70 + Điều 8 nữa mà theo Performance Appraisal khu vực công Singapore: **Work Review** (mục tiêu công việc + Key Result, gắn OKR, cấp trên chấm mức 1–5 có trọng số) · **Competencies AIM** (Analytical/Influence/Motivation) · **Core Values ISE** (Integrity/Service/Excellence) · **Overall Grade A–E** (Outstanding→Unsatisfactory, đề xuất tự động từ Hiệu suất 60% + Năng lực 25% + Giá trị 15%, cấp trên hiệu chỉnh được — xếp hạng tương đối, không quota cứng) · **CEP** (tiềm năng, TÁCH RIÊNG khỏi điểm) · **Development (CFR/IDP)**. Dữ liệu lưu trong `person.sg`. `computeSG`/`sgGradeInfo`/`defaultSG` export từ file; App: `scoreOf = isSG?computeSG:computePerson`, `dist` thêm E, `result`/thẻ điểm/`computed` theo isSG; seed `person.sg` cho 5 cán bộ mẫu. Tab Đánh giá khi isSG render `<SingaporeAppraisal>` (giữ section Thông tin, ẩn Nhóm I/II/Điều 8/aside VN). Tab Tổng quan khi isSG render `<SingaporeDashboard>` (phân bố Grade A–E, bảng xếp hạng + CEP, TB theo phòng; ẩn DashboardCharts/Mẫu 1A/deptSummary/xu hướng VN). **Word**: `exportSGAppraisal` (exporters.js) + `doSGWord` (App) — phiếu .docx theo bố cục Singapore. Lưu ý: bộ tiêu chí dân cử `CRITERIA_SG` (Nhóm I) giờ chỉ còn dùng cho nhãn Mẫu, không render trong bản SG.
- **Singapore: thêm Tầng A "Bảng điểm THIẾT CHẾ" (TCMR) + Cải tiến: viết lại Nhóm II theo NĐ 335/2025**. *Nghiên cứu có nguồn (TCMR/MND, SPOR/MOF, PSD/CEP, NĐ 335/2025/NĐ-CP).* (1) **Bản Singapore — Tầng A**: `SingaporeAppraisal.jsx` thêm `SingaporeInstitution` + `SG_INST_KPI_DEFAULT`/`instBand`/`INST_BAND` — bảng điểm KPI của CƠ QUAN (mô phỏng Town Council Management Report Singapore): 5 chỉ số (phục vụ kỳ họp · văn bản đúng hạn · xử lý kiến nghị cử tri · hài lòng phục vụ · minh bạch & quản trị), mỗi chỉ số xếp **dải màu Xanh/Vàng/Đỏ riêng** (không gộp 1 điểm), ngưỡng cấu hình, quản trị nhập số liệu. Nguyên tắc Singapore: **đại biểu dân cử KHÔNG chấm điểm cá nhân** — chỉ đo thiết chế (Tầng A) + công chức (Tầng B đã có). App: state `instKpi` lưu theo kỳ (autosave/handleManualSave/loadPeriod/copyFromPeriod), render `<SingaporeInstitution>` + `<SingaporeDashboard>` ở tab Tổng quan bản SG. (2) **Bản Cải tiến — Nhóm II**: cơ chế trùng **NĐ 335/2025** (hiệu lực 01/01/2026: (a+b+c)/3, −25%/lần, 70đ) nên GIỮ công thức, chỉ viết lại câu chữ: nhãn rõ hơn (SL được giao/SL hoàn thành/Số lần sai sót lớn/Số lần trễ hạn) + **tooltip định nghĩa thao tác + ví dụ** (MiniNum thêm `hint`/`note`), **nhãn mức** theo số lần (`DEDUCT_LABEL`: 0→100% "Đạt hoàn toàn"…≥4→0%), hiển thị Khối lượng a=%, **giải thích công thức (a+b+c)/3** trong ghi chú, ô KR rõ "tiêu chuẩn nghiệm thu", và **ô "Miễn trừ khách quan"** (`exemptNote`) NĐ 335 cho phép. Áp cho `renderTask335Row` (Cải tiến hiển thị đầy đủ; Cổ điển hưởng nhãn/tooltip rõ hơn).
- **Nhóm II: tách cột Tự ĐG ↔ Cấp duyệt cho từng nhiệm vụ (Cổ điển & Cải tiến)**: mỗi nhiệm vụ giờ có 2 cột số liệu (SL hoàn thành · Số lần sai sót · Số lần trễ hạn) — *Tự ĐG* (cán bộ nhập) và *Cấp duyệt* (cấp trên rà/sửa, dùng để xếp loại); "Số lượng giao" dùng chung. Cấp duyệt **mặc định kế thừa** Tự ĐG khi chưa sửa (giống Nhóm I: `mgrX ?? selfX`). Helper `tCompleted/tQuality/tDelays(t, which)`; `task335Score/agg335/taskStats/taskBreakdown` thêm tham số `which='mgr'|'self'`. `computePerson`: tính `k`(mgr)+`kSelf`(self) → `nhomII`(mgr, xếp loại) + `nhomIISelf`(self) → `totalMgr` theo cấp duyệt, `totalSelf` theo tự ĐG; `taskStats` & Điều 8 dùng số cấp duyệt. UI `renderTask335Row`: lưới 2 cột Tự ĐG/Cấp duyệt + **dòng "Điểm nhiệm vụ: Tự ĐG x% · Cấp duyệt y% · trạng thái"** cho từng việc (giải quyết phản ánh "chưa thấy chấm điểm/phê duyệt ở Nhóm II"). Quyền: khách/admin sửa cả 2 cột; trưởng phòng sửa cột Cấp duyệt (phòng mình); cán bộ sửa cột Tự ĐG. Phiếu Word (`doWord`) dùng số **Cấp duyệt**. Trường mới `mgrCompleted/mgrQualityIssues/mgrDelays` (không lưu sẵn — fallback self, tương thích dữ liệu cũ & demo). Bản Singapore không ảnh hưởng (dùng `computeSG` riêng).
- **Đồng bộ module theo phiên bản (Hướng dẫn · Danh mục · Quản trị/tài liệu)**: (1) **Tab Hướng dẫn** version-aware: thêm banner "Đang xem hướng dẫn cho phiên bản…" + tóm tắt; bọc toàn bộ mục QĐ-1053 trong `{!isSG}` và thêm bộ **mục hướng dẫn riêng cho bản Singapore** (tổng quan 2 tầng · bảng điểm thiết chế TCMR · phiếu cá nhân Work Review/AIM/ISE · công thức 60/25/15 & Grade A–E · CEP · phát triển + nguồn). Nút "Tải sổ tay PDF" ẩn ở bản SG. (2) **Tab Danh mục** (Nhóm II) **ẩn ở bản Singapore** (nút tab + render guard `!isSG`); tự chuyển về Tổng quan nếu đang ở Danh mục khi đổi sang SG. (3) **Quản trị**: mô tả 2 thẻ + nhãn phiên bản đang dùng theo `version`; `doExportSystemDoc`/`doExportOKRMethod` truyền `version`. `exportSystemTechPDF(version)` (đổi chữ ký, dùng `SYSTEM_DOC_DATA` nội bộ) thêm dòng "Phiên bản đang dùng". `exportOKRMethodPDF(unit, version)`: bản SG xuất **tài liệu nghiệp vụ riêng** (2 tầng, dải màu, AIM/ISE, A–E, CEP, không chấm điểm đại biểu); bản Cải tiến thêm ghi chú khác biệt (AIM/ISE + OKR + NĐ 335).
- **Bản mô tả sáng kiến (file Word)**: tạo `Ban-mo-ta-sang-kien-OKR-KPI.docx` ở thư mục gốc — báo cáo mô tả sáng kiến trình Hội đồng khoa học (thông tin chung · thực trạng · mục tiêu · mô tả chi tiết: 3 phiên bản, tách Tự ĐG/Cấp duyệt, OKR · tính mới · hiệu quả · phạm vi · kết luận). Sinh bằng thư viện `docx` qua script tạm (đã xóa script). Tác giả mặc định "Hà Ngọc Sơn — Phó Chánh Văn phòng", để trống ngày tháng/số liệu hiệu quả để điền.
- **Làm lại Hướng dẫn chi tiết + cảnh báo chênh điểm/loại**: thêm helper dùng chung `catalogForGuide()` (gộp 52 mục danh mục theo nhóm + nhãn Mẫu áp dụng qua `MAU_OF_TYPE`/`defaultTypesOfId`). Tab Hướng dẫn: mục 4 thêm "3 điểm cần hiểu đúng về công thức" (hệ số×số lượng; b/c chỉ tính trên phần đã HT, toàn bộ HT=0→66,7%; vượt mức là điều kiện loại A); thêm mục **4.1 bảng danh mục đầy đủ 52 mục** (mã/tên/sản phẩm/cấp độ/hệ số/Mẫu) và **4.2 ví dụ tính điểm xuyên suốt** (1 công chức (a+b+c)/3 tính tới xếp loại + kiểm Điều 8; 1 lãnh đạo (a+b+c+d+đ+e)/6; 5 tình huống ngắn); mục 7 nêu cảnh báo chênh lệch. Sổ tay PDF (`exportGuidePDF(unit, catalogGroups)`) đồng bộ: thêm box 3 điểm, **Phụ lục A** (bảng 52 mục), **Phụ lục B** (ví dụ xuyên suốt), ghi chú cảnh báo. **Phản biện công thức (theo lựa chọn người dùng): GIỮ NGUYÊN cách tính, chỉ giải thích rõ + thêm cảnh báo** — `App.jsx` aside hiện ô đỏ "Chênh lệch điểm số và xếp loại" khi `gradeFromScore(totalMgr)` cao hơn `grade` thực tế (Điều 8 hạ mức / kỷ luật).
